{
  "name": "@bodhi/web-voice-client",
  "version": "0.1.0",
  "private": true,
  "description": "Browser voice client for bodhi voice sessions: PCM capture/playback, mic gating, barge-in flush, playback-state handshake, typed protocol dispatch. Built to dist/ (ESM + declarations); consumed via file:/link: deps.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bodhi/client-protocol": "link:../client-protocol"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ]
}
